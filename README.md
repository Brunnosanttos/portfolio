# Portfólio · Bruno Santos

Site no ar: https://brunosantos-dev.vercel.app

Site pessoal: um único `index.html` sem framework, com CSS e JavaScript embutidos, e capturas dos projetos em `img/`.

- Português por padrão, botão PT/EN no topo.
- Deploy estático na Vercel. Todo push na branch `main` publica a nova versão.

## Rodar localmente

Qualquer servidor estático funciona:

```bash
npx serve .
```
